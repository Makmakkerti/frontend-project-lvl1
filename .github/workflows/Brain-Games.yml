name: Brain-Games

on: 
  - push

jobs:
  build:
    # The type of machine to run the job on
    runs-on: ubuntu-latest

    strategy:
      # Node versions list
      matrix:
        node-version: [13.x]

    steps:
      - name: Run CI
        # Run CI
        run: make ci
      - name: Run linter
        # Run Linter
        run: make lint
